# Example input file

exawind:
  kynema_ugf_inp:
    - sphere-ugf.yaml
  kynema_sgf_inp: sphere-sgf.inp
  num_timesteps: 10

  # Variables for overset exchange
  ugf_vars:
    - velocity
    - pressure

  sgf_cell_vars:
    - velocity

  sgf_node_vars:
    - p
